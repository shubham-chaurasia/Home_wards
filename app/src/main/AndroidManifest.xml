<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.splash">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".signup.PhoneAuthenticationScreen"></activity>
        <activity android:name=".signup.OtpVarificationScreen" />
        <activity android:name=".signup.SignupScreen" />
        <activity android:name=".signup.LoginScreen" />
        <activity android:name=".dashboard.Tab1" />
        <activity android:name=".signup.StartUpScreen" />
        <activity android:name=".Home" />
        <activity android:name=".intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>